<Window x:Class="neTiPx.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Config" Height="420" Width="680" WindowStartupLocation="CenterScreen">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl x:Name="TabControlMain" Grid.Row="0">
            <TabItem Header="Adapter Auswahl">
                <Grid Margin="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="Wähle bis zu 2 physische Netzwerkadapter:" FontWeight="Bold" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,8,0,8">
                        <StackPanel x:Name="AdaptersPanel" />
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem Header="IP Settings">
                <Grid Margin="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button x:Name="BtnAddIpTab" Width="100" Margin="2" Click="BtnAddIpTab_Click">Neuer IP-Tab</Button>
                        <Button x:Name="BtnRemoveIpTab" Width="100" Margin="2" Click="BtnRemoveIpTab_Click">Tab löschen</Button>
                        <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray">(max. 10)</TextBlock>
                    </StackPanel>

                    <TabControl x:Name="IpTabsControl" Grid.Row="1" />
                </Grid>
            </TabItem>
            <TabItem Header="Tools">
                <Grid Margin="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TabControl x:Name="ToolsTabControl" Grid.Row="1">
                        <TabItem Header="Ping">
                            <Grid Margin="6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
                                    <Button x:Name="BtnAddPingEntry" Width="110" Margin="2" Click="BtnAddPingEntry_Click">Ping hinzufügen</Button>
                                    <Button x:Name="BtnResetAllPingEntries" Width="110" Margin="2" Click="BtnResetAllPingEntries_Click">Reset Alle</Button>
                                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0" Foreground="Gray">(max. 6 • Spaltenbreite mit Maus anpassen)</TextBlock>
                                </StackPanel>

                                <!-- Resizable column headers -->
                                <Grid Grid.Row="1" Margin="0,0,0,6" x:Name="PingHeaderGrid">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="60" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="120" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="100" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" FontWeight="Bold" Padding="4,0">Aktiv</TextBlock>
                                    <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />

                                    <TextBlock Grid.Column="2" FontWeight="Bold" Padding="4,0">IP-Adresse</TextBlock>
                                    <GridSplitter Grid.Column="3" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />

                                    <TextBlock Grid.Column="4" FontWeight="Bold" Padding="4,0">Start</TextBlock>
                                    <GridSplitter Grid.Column="5" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />

                                    <TextBlock Grid.Column="6" FontWeight="Bold" Padding="4,0">Fehlend</TextBlock>
                                    <GridSplitter Grid.Column="7" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />

                                    <TextBlock Grid.Column="8" FontWeight="Bold" Padding="4,0">Verlust %</TextBlock>
                                    <GridSplitter Grid.Column="9" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />

                                    <TextBlock Grid.Column="10" FontWeight="Bold" Padding="4,0">Status</TextBlock>
                                    <GridSplitter Grid.Column="11" Width="3" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="LightGray" />
                                </Grid>

                                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="PingEntriesPanel" />
                                </ScrollViewer>
                            </Grid>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>
            <TabItem Header="Info">
                <ScrollViewer Margin="6" VerticalScrollBarVisibility="Auto">
                    <StackPanel Orientation="Horizontal" Margin="6">
                        <Image x:Name="InfoImage" Width="96" Height="96" Margin="6" Stretch="Uniform" />
                        <StackPanel Orientation="Vertical" Margin="6" Width="380">
                            <TextBlock x:Name="InfoText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Padding="6" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="BtnApply" Width="80" Margin="4" Click="BtnApply_Click">Anwenden</Button>
            <Button x:Name="BtnSave" Width="80" Margin="4" Click="BtnSave_Click">Speichern</Button>
            <Button x:Name="BtnCancel" Width="80" Margin="4" Click="BtnClose_Click">Schliessen</Button>
        </StackPanel>
    </Grid>
</Window>
